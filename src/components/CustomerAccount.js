import { getCurrentCustomer, getMyOrders } from "../services/api.js";

const safe=value=>String(value??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
export function CustomerAccount(){return `<div class="modal fade" id="customerProfileModal" tabindex="-1" aria-labelledby="customerProfileTitle" aria-hidden="true"><div class="modal-dialog modal-dialog-centered"><div class="modal-content"><div class="modal-header bg-dark text-white"><h2 class="modal-title h5" id="customerProfileTitle"><i class="bi bi-person-circle me-2"></i>My Profile</h2><button class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button></div><div class="modal-body" id="customerProfileContent"><div class="text-center py-5"><span class="spinner-border"></span></div></div></div></div></div>`;}
export function setupCustomerAccount(){document.getElementById("customerProfileModal")?.addEventListener("show.bs.modal",async()=>{const box=document.getElementById("customerProfileContent");box.innerHTML='<div class="text-center py-5"><span class="spinner-border"></span></div>';try{const[profile,orders]=await Promise.all([getCurrentCustomer(),getMyOrders()]);const customer=profile.customer,list=orders.orders||[];box.innerHTML=`<div class="text-center mb-4"><div class="customer-profile-avatar mx-auto">${safe(customer.name?.[0]||"C")}</div><h3 class="h4 mt-3 mb-1">${safe(customer.name)}</h3><span class="badge text-bg-success">Active customer</span></div><dl class="customer-profile-details"><div><dt><i class="bi bi-envelope me-2"></i>Email</dt><dd>${safe(customer.email)}</dd></div><div><dt><i class="bi bi-telephone me-2"></i>Phone</dt><dd>${safe(customer.phone)}</dd></div></dl><div class="row g-2 mt-3"><div class="col-6"><div class="profile-stat"><strong>${list.length}</strong><span>Total orders</span></div></div><div class="col-6"><div class="profile-stat"><strong>${list.filter(o=>o.orderStatus==="delivered").length}</strong><span>Completed</span></div></div></div><button class="btn btn-warning w-100 mt-4" id="profileViewOrders"><i class="bi bi-box-seam me-2"></i>View My Orders</button>`;document.getElementById("profileViewOrders")?.addEventListener("click",()=>{window.dispatchEvent(new CustomEvent("nutridust:open-orders"));});}catch(error){box.innerHTML='<div class="alert alert-danger">Unable to load your profile. Please sign in again.</div>';}});}

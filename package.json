{
  "name": "nutridustfoods-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mobile:prepare": "node scripts/prepare-mobile-web.mjs",
    "mobile:sync": "npm run mobile:prepare && node scripts/sync-mobile.mjs",
    "apk:customer": "npm run mobile:sync && cd mobile/customer/android && gradlew.bat assembleDebug",
    "apk:rider": "npm run mobile:sync && cd mobile/rider/android && gradlew.bat assembleDebug",
    "apk:admin": "npm run mobile:sync && cd mobile/admin/android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "axios": "^1.19.0",
    "bootstrap": "^5.3.8",
    "bootstrap-icons": "^1.13.1"
  }
}
